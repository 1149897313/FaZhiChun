<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">


    <style name="Theme.private" parent="Theme.AppCompat.Light">
        <!-- 复写3个主要的色彩. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--背景色-->
        <item name="android:windowBackground">@color/windowsBackground</item>

        <!--设置没有Title-->
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <!--ToolBar高度-->
        <item name="actionBarSize">@dimen/toolBarSize</item>

        <!--初始化自己定义的3个属性值-->
        <item name="toolbarTitleSize">18sp</item>
        <item name="toolbarTitleColor">@color/textColorLight</item>
        <item name="toolbarMenuIconColor">@color/white_alpha_224</item>

    </style>

    <style name="AppTheme.Base" parent="Theme.private" />

    <!--自定义Dialog显示样式-->
    <style name="ThemeCustomDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>


    <style name="MyDialogStyle">
        <!--你的layout背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <!--屏幕其他地方是否模糊变黑-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>


</resources>